import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

interface Props {}

const MaskedScreen = (props: Props) => {
  return (
    <View>
      <Text>MaskedScreen</Text>
    </View>
  );
};

export default MaskedScreen;

const styles = StyleSheet.create({});
